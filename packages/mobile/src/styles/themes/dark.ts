import { DefaultTheme } from 'styled-components';
import { ThemeID } from '../../types';

const dark: DefaultTheme = {
  id: ThemeID.DARK,
  colors: {
    primary: '#f0f',
  },
};

export default dark;
